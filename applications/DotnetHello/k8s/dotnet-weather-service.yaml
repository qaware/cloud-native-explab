apiVersion: v1
kind: Service
metadata:
  labels:
    app: dotnet-weather-service
  name: dotnet-weather-service
spec:
  type: loadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: dotnet-weather
