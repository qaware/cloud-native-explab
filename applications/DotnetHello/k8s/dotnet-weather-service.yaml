apiVersion: v1
kind: Service
metadata:
  labels:
    app: dotnet-weather
  name: dotnet-weather-service
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: dotnet-weather
